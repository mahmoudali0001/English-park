import React from "react";

const logo = (props) => (
  <div className={`md:mt-1 text-2xl ${props.classes}`}>
    <div>
      English <span className="text-orange-300">Park</span>
    </div>
  </div>
);

export default logo;
